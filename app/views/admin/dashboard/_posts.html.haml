%table
  %tr
    %th Otsikko
    %th Kirjoittaja
    %th Luotu
    %th
    %th
    %th
    %th Älä näytä
    %th Viikon aihe
    %th Toimituksen valinta
  - @posts.each do |post|
    %tr
      %td= post.title
      %td
        - if post.user != nil
          = post.user.end_user_name
      %td= post.created_at.to_s(:long)
      %td
        = link_to 'Näytä', post
      %td
        = link_to 'Muokkaa', edit_post_path(post)
      %td
        = link_to 'Poista', post, :confirm => 'Are you sure?', :method => :delete
      = semantic_form_for post do |f|
        %td
          = f.input :published, :label => ""
        %td
          = f.input :weektopic, :label => ""
        %td
          = f.input :editors_pick, :label => ""
        %td
          = f.input :visitor_post, :label => ""
        %td
          = f.submit "Päivitä"
          
%br/

%p
  =will_paginate @posts