/ table header stuff here
%table
  - @users.each do |u|
    %tr
      %td= link_to_if(can?(:read, User), u.end_user_name, user_path(u.id)) {}
      %td= u.email
      %td= u.roles.collect{|r| r.name}
      %td= link_to_if(can?(:edit, User), "Edit", edit_user_path(u.id)) {}
      %td= link_to_if(can?(:delete, u), "Delete", u, :confirm => "Are you sure?", :method => :delete) {}
